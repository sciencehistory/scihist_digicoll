<%# customized from blacklight mainly to eliminate those ugly `|` punctuation separators!
    replacing with spans so we can add margin.

https://github.com/projectblacklight/blacklight/blob/v6.7.2/app/views/kaminari/blacklight_compact/_paginator.html.erb
%>

<%# The container tag
  - available local variables
    current_page:  a page object for the currently displayed page
    num_pages:     total number of pages
    per_page:      number of items to fetch per page
    remote:        data-remote
    paginator:     the paginator that renders the pagination tags inside

   Paginator now using the Bootstrap paginator class

   page_entries_info is passed into the paginate call, e.g. :

   > paginate collection, :page_entries_info => page_entries_info(collection), :theme => :blacklight_compact

   As of Kaminari 0.15, this paginator doesn't have access to the original collection/scope, so it can't render the page entries info directly.
-%>
<% if total_pages > 1 -%>
  <%# #render checks if total_pages > 1, so we can't put our fallback
  in here .. -%>
  <%= paginator.render do -%>
    <div class="page_links">
      <span class="prev"><%= prev_page_tag %></span>
      <span class="page_entries">
        <%= page_entries_info %>
      </span>
      <span class="next"><%= next_page_tag %></span>
    </div>
  <% end -%>
<% else -%>
    <div class="page_links">
      <span class="page_entries">
        <%= page_entries_info %>
      </span>
    </div>
<% end -%>
